/** @type {import('next').NextConfig} */
// Converted to CommonJS to avoid ESM warnings in Next dev environment.
// Removed experimental.turbo to prevent "Unrecognized key" warnings.
const nextConfig = {
  // If you later enable specific turbo rules, ensure your Next version supports them.
};

module.exports = nextConfig;
